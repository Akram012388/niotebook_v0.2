Read the current CLAUDE.md to identify which workstream this instance owns. Then:
1. List GitHub Issues for this workstream's label (e.g., `ws-a`) using `gh issue list --label <label>`.
2. Show which issues are open vs closed.
3. Run `git log --oneline -10` to show recent commits.
4. Summarize: tasks completed, tasks remaining, any blockers.
